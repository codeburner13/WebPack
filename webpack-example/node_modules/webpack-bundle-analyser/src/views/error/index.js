import { setTitle } from 'meta';

export default async (render, options) => {
  setTitle('Error');

  const [name] = options.args;
  const template = await import('./view.jade');
  render(template.default, { name });
};
