if errors.length === 0
  section.hero.is-success
    .hero-body
      .container
        h1.title
          | No errors. ✅
        h2.subtitle
          | Time to celebrate.
else
  section.hero.is-danger
    .hero-body
      .container
        h1.title
          = errors.length
          |  error
          if errors.length !== 1
            | s
          |  found.
        h2.subtitle
          | Get ready to get your hands dirty.

each error in errors
  .container
    br
    br
    h4.title.is-4
      = error.header
    pre
      = error.text
    h5.subtitle.is-6
      = error.footer
